# ===========================================
# 環境変数設定ファイル（サンプル）
# ===========================================
# このファイルを .env にコピーして使用してください
# cp .env.example .env
# ===========================================

# ========== データベース設定 ==========
# PostgreSQL接続情報
POSTGRES_DB=fx_trading
POSTGRES_USER=fx_user
POSTGRES_PASSWORD=fx_password123
DB_HOST=db  # Docker環境では 'db'、ローカルでは 'localhost'
DB_PORT=5432

# ========== API キー設定 ==========
# OpenAI API（ChatGPT）
# https://platform.openai.com/api-keys から取得
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# Claude API（Anthropic）- 将来の拡張用
# https://console.anthropic.com/ から取得
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx

# GMOコイン API（将来の本番環境用）
GMO_API_KEY=your_gmo_api_key_here
GMO_API_SECRET=your_gmo_api_secret_here

# ========== Django設定 ==========
# 本番環境では必ず変更してください
DJANGO_SECRET_KEY=django-insecure-change-this-in-production
DJANGO_DEBUG=True  # 本番環境では False に設定
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# ========== その他のサービス ==========
# Redis設定（キャッシュ・Celery用）
REDIS_URL=redis://redis:6379/0

# ========== 分析設定 ==========
# 分析に使用するLLMモデル
LLM_MODEL=gpt-4  # gpt-3.5-turbo, gpt-4, claude-3-opus など
LLM_TEMPERATURE=0.7  # 0.0（確定的）～ 1.0（創造的）
LLM_MAX_TOKENS=2000

# デフォルトの分析期間
DEFAULT_ANALYSIS_PERIOD=3mo  # 1d, 5d, 1mo, 3mo, 6mo, 1y, 5y, max
DEFAULT_ANALYSIS_INTERVAL=1d  # 1m, 5m, 15m, 30m, 1h, 1d

# ========== セキュリティ設定 ==========
# CORS設定（フロントエンドのURL）
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# ========== ログ設定 ==========
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FILE_PATH=/var/log/fx_trading/app.log